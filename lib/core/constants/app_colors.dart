import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const navy        = Color(0xFF0F1C3A);  // Main background / header
  static const navyLight   = Color(0xFF1A2C4E);  // Card background
  static const gold        = Color(0xFFC9A84C);  // Primary accent
  static const goldLight   = Color(0xFFE8C97A);  // Hover / secondary gold
  static const goldMuted   = Color(0x26C9A84C);  // Gold with 15% opacity

  // Neutrals
  static const white       = Color(0xFFFFFFFF);
  static const offWhite    = Color(0xFFF8F6F0);
  static const surface     = Color(0xFFF2EDE4);

  // Semantics
  static const success     = Color(0xFF2E7D32);
  static const warning     = Color(0xFFE65100);
  static const error       = Color(0xFFC62828);

  // Text
  static const textPrimary    = Color(0xFF1A1A1A);
  static const textSecondary  = Color(0xFF6B6B6B);
  static const textOnDark     = Color(0xFFFFFFFF);
  static const textOnDarkMuted = Color(0x99FFFFFF);   // 60% white
}
